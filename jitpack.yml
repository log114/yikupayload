jdk:
  - openjdk11

before_install:
  - echo "当前磁盘空间:"
  - df -h
  - echo "清理缓存..."
  - rm -rf ~/.gradle/caches
  - mkdir -p ~/.gradle/caches
  - chmod 777 ~/.gradle/caches

install:
  - ./gradlew clean build -x test --no-daemon --stacktrace -Dorg.gradle.daemon=false